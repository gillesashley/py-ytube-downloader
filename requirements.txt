# yt-dlp intentionally unpinned: YouTube changes break it; every rebuild gets the latest.
yt-dlp
Django==6.1
whitenoise==6.9.0
# dev-only, for type checking (pyright); not needed at runtime
# django-stubs
